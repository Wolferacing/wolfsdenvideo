class Playlist {
  constructor() {
    this.hostUrl = location.host;
    this.parseAttributes();
  }
  parseAttributes
}

require(['polyfills', 'commands', 'responses' ], function(data) {
    new Playlist();
});
